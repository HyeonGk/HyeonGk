import kind from '@enact/core/kind';

const HarvestView = kind({
    name: 'HarvestView',
    render: () => (
        <div>
            <h1>하비봇 페이지</h1>
            <p>여기서는 하비봇 관련 내용을 표시합니다.</p>
        </div>
    )
});

export default HarvestView;
